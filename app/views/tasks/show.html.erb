<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p></p>
<p></p>
<%= link_to "Back to list", tasks_path %> <p></p> <%= link_to "Edit this task", edit_path %>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href="/tasks/${@task}"></a>